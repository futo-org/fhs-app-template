// The app ships as a static SPA (see adapter-static in vite.config.ts):
// no server-side rendering and no prerendered pages, just the index.html
// fallback plus client-side routing.
export const ssr = false;
export const prerender = false;
